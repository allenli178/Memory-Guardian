// https://nuxt.com/docs/api/configuration/app-config
export default defineAppConfig({
  ui: {
    colors: {
      primary: "amber",
      neutral: "stone",
    },
    button: {
      rounded: "xl",
      size: "xl",
      padding: "xl",
    },
    card: {
      rounded: "2xl",
    },
  },
});
